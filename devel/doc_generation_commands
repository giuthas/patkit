# Generate docs with pdoc ...
# TODO 0.15: see about removing the * from below
pdoc -d numpy -o docs/api src/patkit/*

# and some diagrams to be linked as well.
# Needs Graphviz to work properly
pyreverse -o png -p patkit -d docs/api src/patkit
